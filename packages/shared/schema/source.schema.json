{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://is-it-local.dev/schema/source.schema.json",
  "title": "Source",
  "description": "A piece of evidence retrieved from a provider that supports a classification.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "provider", "url", "retrieved_at"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Stable unique identifier for the source."
    },
    "provider": {
      "type": "string",
      "description": "Origin of the source (e.g. foursquare, web_search, corporate_registry)."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "URL where the evidence was found."
    },
    "retrieved_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the source was retrieved."
    },
    "snippet": {
      "type": "string",
      "description": "Relevant excerpt supporting the classification."
    }
  }
}
